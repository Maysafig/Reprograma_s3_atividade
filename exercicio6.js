//6 - crie um algoritmo que converte dias em horas, quando recebe um número de dias.

let dias 

function converterDias(dias) {
     const diasEmHoras= dias * 24
console.log(`${dias} dias tem ${diasEmHoras} horas.`)
}

converterDias(2)